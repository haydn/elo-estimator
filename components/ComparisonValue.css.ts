import { style } from "@vanilla-extract/css";

export const none = style({
  background: "red",
  padding: "0 0.5em",
});

export const low = style({
  background: "orange",
  padding: "0 0.5em",
});
